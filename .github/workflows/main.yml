name: CI Pipeline - Main

on:
  workflow_dispatch:

jobs:
  pre-main:
    if: github.ref == 'refs/heads/pre-main'
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run test-dev Update
        run: |
          sudo su - egap -c '~/.local/bin/odoo-update odoo --force'
