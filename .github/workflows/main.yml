name: Manual Trigger Placeholder

on:
  push:
    branches:
      - main


jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: No operation
        run: echo "This workflow is intentionally empty."
